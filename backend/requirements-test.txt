# Test-only dependencies — not needed in production.
# Install with: pip install -r requirements-test.txt
#
# Note: all production deps must also be installed first:
#   pip install -r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0        # already in requirements.txt; listed here for explicitness
anyio[trio]>=4.0.0   # lets asyncio_mode=auto work in pytest.ini
