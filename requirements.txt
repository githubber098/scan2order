# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
aiofiles>=24.0.0

# HTTP client (store API calls)
httpx>=0.27.0

# Session storage (SQLite, stdlib — no external service needed)

# OCR
pytesseract>=0.3.13
Pillow>=11.0.0

# Playwright (headless Chromium for store login relay)
playwright>=1.44.0

# Optional: Groq LLM ranking fallback
# groq>=0.8.0  # auto-installed if GROQ_API_KEY is set; safe to add explicitly
