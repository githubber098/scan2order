# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
aiofiles>=24.0.0

# HTTP client (store API calls)
httpx>=0.27.0

# Redis session storage
upstash-redis>=1.3.0

# OCR
pytesseract>=0.3.13
Pillow>=11.0.0

# Optional: Groq LLM ranking fallback
# groq>=0.8.0  # auto-installed if GROQ_API_KEY is set; safe to add explicitly
